---
description: format the codes and run type checking
---

1. Run `bun run format` to lint, auto-fix, and format the code with Biome.
2. Run `bun run typecheck` to verify TypeScript types with no errors.